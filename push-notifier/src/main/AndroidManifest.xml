<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="at.sbaresearch.push_notifier">

    <permission
            android:name="at.sbaresearch.android.gcm.intent.SEND"
            android:label="@string/permlab_send_push"
            android:description="@string/permdesc_send_push"
            android:protectionLevel="dangerous" />

    <uses-permission android:name="at.sbaresearch.android.gcm.intent.SEND"/>

    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity
                android:name=".PushNotifyActivity"
                android:label="@string/app_name"
                android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <receiver
            android:name="at.sbaresearch.push_notifier.MqttBackendReceiver"
            android:exported="true"
            android:permission="at.sbaresearch.android.gcm.mqtt.intent.SEND">
            <intent-filter>
                <action android:name="at.sbaresearch.android.gcm.mqtt.intent.RECEIVE"/>
                <category android:name="at.sbaresearch.push_notifier"/>
            </intent-filter>
        </receiver>
        <!--
        <service
            android:name=".BackendReceiverService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="at.sbaresearch.android.gcm.mqtt.intent.RECEIVE"/>
            </intent-filter>
        </service>
        -->
    </application>

</manifest>